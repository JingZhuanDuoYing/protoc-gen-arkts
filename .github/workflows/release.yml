name: Release

on:
  release:
    types: [published]
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build-and-upload:
    name: Build and upload binaries
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            artifact_name: protoc-gen-arkts
            asset_name: protoc-gen-arkts-linux-x86_64
          - os: macos-latest
            artifact_name: protoc-gen-arkts
            asset_name: protoc-gen-arkts-macos-aarch64
          - os: macos-13
            artifact_name: protoc-gen-arkts
            asset_name: protoc-gen-arkts-macos-x86_64
          - os: windows-latest
            artifact_name: protoc-gen-arkts.exe
            asset_name: protoc-gen-arkts-windows-x86_64.exe
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up Rust
        run: rustup update stable
        
      - name: Build release binary
        run: cargo build --release
        
      - name: Rename binary (Unix)
        if: matrix.os != 'windows-latest'
        run: cp target/release/${{ matrix.artifact_name }} ${{ matrix.asset_name }}
        
      - name: Rename binary (Windows)
        if: matrix.os == 'windows-latest'
        shell: bash
        run: cp target/release/${{ matrix.artifact_name }} ${{ matrix.asset_name }}
          
      - name: Upload Release Asset
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/') || github.event_name == 'release'
        with:
          files: ${{ matrix.asset_name }}
